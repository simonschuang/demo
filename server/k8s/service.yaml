apiVersion: v1
kind: Service
metadata:
  name: agent-server-service
  namespace: agent-system
  labels:
    app: agent-server
  annotations:
    metallb.universe.tf/loadBalancerIPs: "100.74.51.235"
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: agent-server
